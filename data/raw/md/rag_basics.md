# Retrieval-Augmented Generation

Retrieval-Augmented Generation, often called RAG, combines a language model with a retrieval system. The retriever fetches relevant evidence from an external knowledge base, and the generator uses that evidence to produce a more grounded answer. RAG is useful because it can reduce unsupported statements and lets a system work with documents that were not seen during model pretraining.
